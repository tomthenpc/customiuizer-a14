package tv.withaibuild.customiuizer.mods.utils.gesture

/**
 * High-level state of a single gesture session.
 */
enum class GestureState {
    IDLE,
    TRACKING,
    SLIDING_BRIGHTNESS,
    SLIDING_VOLUME,
}
