# A14 兼容矩阵

## 平台

| 平台 | 状态 | 规则 |
|---|---|---|
| HyperOS 1 / Android 14 | 主支持 | SDK 34、目标 ROM 能力探测、实机证据分级 |
| MIUI / Android 13 | 不支持 | 使用 A13 仓库 |
| Android 15/16 | 不支持 | 不把未来版本猜测加入生产路径 |

## libxposed

| 能力 | 状态 | 规则 |
|---|---|---|
| API 101 | 最低生产基线 | 所有必经路径可加载 |
| API 102 | 条件能力 | 类型和初始化隔离、能力探测、安全降级 |

跨版本工作必须是独立 `PORT` 任务，并记录 API、ROM、生命周期和资源差异。不维护逐文件 parity。
