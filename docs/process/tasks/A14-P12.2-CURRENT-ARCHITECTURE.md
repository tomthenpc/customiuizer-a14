# A14 Task Slice — P12.2 CURRENT Architecture

```text
TaskId: A14-P12.2
Repository: tomthenpc/customiuizer-a14
Branch: devin/a14-rom-intelligence-audit
ExpectedBaseCommit: 9aef46d25872d77b93203b203b1cade3f1f2f12f
RiskTier: R1
```

## Preflight

确认仓库与分支：

```powershell
git remote get-url origin
git branch --show-current
git rev-parse HEAD
git rev-parse --abbrev-ref '@{u}'
git status --short
python tools/check_automation_state.py
```

要求：

```text
origin = tomthenpc/customiuizer-a14
branch = devin/a14-rom-intelligence-audit
upstream = origin/devin/a14-rom-intelligence-audit
HEAD = 9aef46d25872d77b93203b203b1cade3f1f2f12f
```

## 唯一目标

创建并机械验证一份与当前源码一致的 A14 CURRENT architecture 文档。

目标文件：

```text
docs/A14_CURRENT_ARCHITECTURE.md
```

允许范围：

```text
docs/A14_CURRENT_ARCHITECTURE.md
tools/tests/test_current_architecture.py
docs/process/tasks/A14-P12.2-CURRENT-ARCHITECTURE.md
TASK_STATE.md
docs/progress/A14_PROGRESS_CURRENT.json
docs/progress/A14_PROGRESS_CURRENT.md
```

禁止修改：

```text
app/src/main/**
现有 runtime 逻辑
Hook 行为
progress_snapshot.py
check_automation_state.py
CI workflow
P12.3 Gesture event contract
P12.4 APK delta
P14 MACHINE_COMPLETE
A13 仓库或 A13 文件
```

## 文档约束

- 文档必须描述当前真实架构，不写未来设计，不把计划状态描述为已经完成。
- EvidenceCommit: `9aef46d25872d77b93203b203b1cade3f1f2f12f` 作为审计基线。
- 引用的 source path 必须在 EvidenceCommit 中存在。
- 引用稳定 symbol，不依赖容易漂移的绝对行号。
- 不把普通叙述当 evidence command。
- 不引用尚未提交的新测试文件作为旧 commit 的源码证据。

## 必须覆盖章节

1. 启动和安装入口
2. 进程与包路由
3. Feature 架构
4. Hook 所有权
5. ClassLoader 和生命周期
6. Java/Kotlin 边界
7. 配置和事件流
8. 验证架构
9. 当前已知限制

## EvidenceCommit

```text
9aef46d25872d77b93203b203b1cade3f1f2f12f
```

## 独立测试

```text
tools/tests/test_current_architecture.py
```

测试至少验证：

1. 文档存在；
2. DocumentKind 为 CURRENT；
3. Repository 和 Branch 正确；
4. EvidenceCommit 是完整 40 位 SHA；
5. EvidenceCommit 存在并且是当前 HEAD 的祖先；
6. 每个引用的 source path 在 EvidenceCommit 中存在；
7. 文档包含所有必需章节；
8. 关键 symbol 在声明的 source path 中真实存在；
9. 文档不得包含 pending/TBD 作为已完成架构结论；
10. 不得引用 A13 路径或仓库；
11. 不得引用不存在的 source path；
12. 不得把 P12.3/P12.4 描述为完成。

## Mutation

临时修改并恢复：

- 损坏 EvidenceCommit，测试必须失败；
- 损坏一个 source path，测试必须失败；
- 删除一个必需章节，测试必须失败；
- 将 A14 路径替换成 A13，测试必须失败；
- 删除一个关键 symbol，测试必须失败。

## 验证

```text
python -m unittest tools.tests.test_current_architecture
python -m unittest discover -s tools/tests -p "test_*.py"
python tools/check_document_contracts.py
python tools/check-invariants.py
python tools/check_automation_state.py
python tools/progress_snapshot.py
python tools/progress_snapshot.py --check
python tools/verify.py fast
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1 -Mode Fast
git diff --check
git status --short
```

## TASK_STATE

P12.2 完成后设置为 `VERIFIED_STATIC`，使用本文档和测试作为证据。
