# A14 Task Slice — P12.3 Gesture Event Contract

```text
TaskId: A14-P12.3
Repository: tomthenpc/customiuizer-a14
Branch: devin/a14-rom-intelligence-audit
ExpectedBaseCommit: ca2556647c4a776a63694259269cdcf5150158e0
RiskTier: R1
```

## Preflight

确认仓库与分支：

```powershell
git remote get-url origin
git branch --show-current
git rev-parse HEAD
git rev-parse --abbrev-ref '@{u}'
git status --short
python tools/check_automation_state.py
```

要求：

```text
origin = tomthenpc/customiuizer-a14
branch = devin/a14-rom-intelligence-audit
upstream = origin/devin/a14-rom-intelligence-audit
HEAD = ca2556647c4a776a63694259269cdcf5150158e0
```

## 唯一目标

创建并机械验证 A14 当前 Gesture event contract 文档。

目标文件：

```text
docs/A14_GESTURE_EVENT_CONTRACT.md
```

允许修改：

```text
docs/A14_GESTURE_EVENT_CONTRACT.md
tools/tests/test_gesture_event_contract.py
docs/process/tasks/A14-P12.3-GESTURE-EVENT-CONTRACT.md
TASK_STATE.md
docs/progress/A14_PROGRESS_CURRENT.json
docs/progress/A14_PROGRESS_CURRENT.md
```

禁止修改：

```text
app/src/main/**
GestureMachine / GestureStateMachine / PhysicalGestureArbiter 等运行时代码
progress_snapshot.py
check_document_contracts.py
check_automation_state.py
CI workflow
P12.4 APK delta
P14 MACHINE_COMPLETE
A13 文件
```

## EvidenceCommit

```text
Base (working tree): ca2556647c4a776a63694259269cdcf5150158e0
Effective EvidenceCommit: d189ad12fc50522ada4772fcb6e5afb510469e01
```

`progress_snapshot.py` 要求 `EvidenceCommit` 必须是严格早于 `HEAD` 的祖先，因此实际使用 `d189ad12`。`ca255664` 仅添加了 P12.2 的架构文档/测试，未修改 `app/src/main` 下的 gesture 源码，所以 `d189ad12` 的 gesture source 与基线一致。

## 文档约束

- 只描述 `d189ad12` 中真实存在的事件和状态语义，不写未来设计。
- 所有 source path 必须在 `EvidenceCommit` 中存在。
- 引用稳定 symbol，不依赖漂移行号。
- 不引用 A13。
- 不把设备未验证内容写成已验证结论。
- P12.4 不得被描述为完成。

## 必须覆盖章节

1. 事件来源
2. 事件数据
3. 状态机
4. 事件顺序
5. 所有权
6. 副作用
7. 清理契约
8. 高频路径约束
9. 已知限制

## 新增独立测试

```text
tools/tests/test_gesture_event_contract.py
```

测试至少验证：

1. 文档存在；
2. DocumentKind 为 CURRENT_CONTRACT（或仓库允许的等价类型）；
3. Repository、Branch、EvidenceCommit 正确；
4. EvidenceCommit 为完整 40 位 SHA，存在且为 HEAD 祖先；
5. 所有 source path 在 EvidenceCommit 中存在；
6. 文档引用的关键 symbol 在相应源码中存在；
7. 所有必需章节存在；
8. prepare/bind/unbind/clear/release 等生命周期动作均被描述；
9. recognized/cancelled 事件的副作用约束存在；
10. P12.4 不得被描述为完成；
11. 不得引用 A13；
12. 不得把设备未验证内容写成已验证结论。

## Mutation

测试必须加入 mutation 校验：

- 删除 cancellation 章节，测试失败；
- 删除 owner cleanup 规则，测试失败；
- 损坏一个 source path，测试失败；
- 损坏 EvidenceCommit，测试失败；
- 将“一次副作用”改成允许重复副作用，测试失败；
- 将 DEVICE_LIFECYCLE_ENTRY_BLOCKED 改成已解决，测试失败。

## 验证

```text
python -m unittest tools.tests.test_gesture_event_contract
python -m unittest discover -s tools/tests -p "test_*.py"
python tools/check_document_contracts.py
python tools/check-invariants.py
python tools/check_automation_state.py
python tools/progress_snapshot.py --write
python tools/progress_snapshot.py --check
python tools/verify.py fast
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1 -Mode Fast
git diff --check
git status --short
```

## TASK_STATE

P12.3 完成后设置为 `VERIFIED_STATIC`，使用本文档和测试作为证据。
