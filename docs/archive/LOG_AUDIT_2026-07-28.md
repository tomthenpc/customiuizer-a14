# LSPosed full.log 快速审查报告 — 2026-07-28

> 审查方式：按 `docs/LSPOSED_FULL_LOG_REVIEW_PROTOCOL_DYNAMIC.md` 快速模式执行；未顺序通读整个日志。
> 原始日志未复制/未提交到仓库。本报告中的本地路径已脱敏。

## 输入与日志定位

- 用户输入类型：目录
- 用户输入路径（脱敏）：`<USER_PROVIDED_LOG_DIR>\r14\r14.13.3\LSPosed_2026-07-27T20_48_50.619383`
- 实际使用日志文件：`full.log`
- 候选 `full.log` 数量：1
- 选择依据：目录根部存在唯一的 `full.log`
- 文件大小：15.38 MiB（16,122,748 bytes）
- 时间范围（日志内最早/最晚时间戳）：`02-04 06:04:44.319`（环形缓冲区旧条目）至 `07-27 20:48:53.227`
- 本次捕获有效区间：`07-27 20:39:04.979` 至 `07-27 20:48:53.227`

## 模块加载矩阵

| 时间 | PID | 进程 | 行为 | 结果 |
| --- | --- | --- | --- | --- |
| 07-27 20:47:28.318 | 28276 | `com.android.settings:remote` | `VectorModuleManager` Loading module `tv.withaibuild.customiuizer.r14` | 成功 |
| 07-27 20:47:28.327 | 28276 | `com.android.settings:remote` | Loading class `cp`（R8 `-repackageclasses` 后的入口） | 成功 |
| 07-27 20:47:28.329 | 28276 | `com.android.settings:remote` | Loaded module `tv.withaibuild.customiuizer.r14` successfully | 成功 |
| 07-27 20:47:49.813 | 28606 | `com.android.systemui` | `VectorModuleManager` Loading module `tv.withaibuild.customiuizer.r14` | 成功 |
| 07-27 20:47:49.821 | 28606 | `com.android.systemui` | Loading class `cp` | 成功 |
| 07-27 20:47:49.824 | 28606 | `com.android.systemui` | Loaded module `tv.withaibuild.customiuizer.r14` successfully | 成功 |

- `system_server`（PID 2889）在本次捕获中未出现 `VectorModuleManager` 加载本模块的记录；`system_server` 从日志开始即存在，未经历 LSPosed 框架重新注入。
- `com.miui.home`（Launcher）未在本次捕获中加载/重启；因此无法从该日志验证 Launcher Hook。
- `com.android.settings:remote` 在加载模块后约 41 秒（`20:48:09.743`）被 `am_proc_died` 正常回收，无 `am_crash`/`am_anr`/`FATAL EXCEPTION`。

## Crash / ANR / 致命信号

| 时间 | PID/进程 | 事件 | 与模块关联性 |
| --- | --- | --- | --- |
| 07-27 20:38:59.292 | 3356 (`init`) | `Fatal signal 6 (SIGABRT)` | 无模块包名/调用链，root `init` 崩溃，与模块无关 |
| 07-27 20:39:07.099 | 8362 (`init`) | `Fatal signal 6 (SIGABRT)` | 同上，root `init` 崩溃，与模块无关 |

- `am_crash`、`am_anr`、`ANR in`、`FATAL EXCEPTION` 在模块进程（PID 28276、28606）中均未出现。
- 未发现可归因于 `tv.withaibuild.customiuizer.r14` 的崩溃或 ANR。

## Hook / 链接 / ClassNotFound 错误

共发现 13 条 `ClassNotFoundException`，均来自系统/ROM/Settings 组件，无一出现在模块调用栈中：

- `system_server`（PID 2889）：
  - `com.android.server.location.LocationPolicyManagerService$Lifecycle`
  - `com.android.server.wm.MiuiDragAndDropStubImpl$DragNotAllowPackages`
- `com.android.settings`（PID 5824 / 28606）与 `com.android.mms`：
  - `com.miui.zeus.landingpage.sdk.LandingPageSDK`
  - `com.android.settingslib.wifi.WifiEntryUtils`
  - `com.android.settingslib.wifi.SlaveWifiUtils`
  - `com.android.settingslib.wifi.PasspointUtils`
  - `com.android.settingslib.bluetooth.BroadcastSourceInfoHandler`

结论：这些异常为 ROM/框架/Cloud 解析在不同机型或版本上的类缺失，日志栈中无 `LSPosed-Bridge`、`cp` 或模块包名，不判定为模块导致。但模块若通过反射查找上述 Settings/WiFi/BT 类，应确认其 fallback 是“未找到则跳过”而非直接向上抛异常。

## RemotePreferences

- 日志中未出现 `RemotePreferences`、`customiuizer_prefs_remote`、`xposed_service` 等关键字。
- 本次审查无法从日志中确认 RemotePreferences 初始化、监听回调或空快照行为，也未发现 RemotePreferences 相关异常。
- 如需验证，应在 `MainModule.java` / `AppHelper.kt` 中增加带明确 tag 的调试日志，或在 API 102 环境下单独复测。

## SystemUI / Launcher / Settings 生命周期

| 进程 | 启动次数 | 死亡次数 | 备注 |
| --- | --- | --- | --- |
| `com.android.systemui` | 9 | 2 | 均在日志早期启动阶段，无崩溃伴随 |
| `com.android.settings`（含 `:remote`） | 19 | 5 | PID 28276 加载模块后正常死亡 1 次 |
| `com.miui.home` | 6 | 0 | 未在捕获中加载本模块 |
| `system_server` | 0 | 0 | 从日志开始持续运行 |

- 未观察到 SystemUI 或 Settings 在模块加载后崩溃/ANR。
- Launcher 在本次日志中未重启，无法验证 Launcher Hook 注入与生命周期。

## Locale / 配置重建

- `AppLocaleController`、`setApplicationLocales`、`applicationLocales` 等模块 Locale 关键字未出现。
- 仅发现 2 条来自 `org.lsposed.manager.ui.activity.MainActivity`（PID 16016）的 `AppCompatDelegate: The Activity's LayoutInflater already has a Factory installed...`，这是 LSPosed 管理器应用的 AppCompat 警告，与模块无关。
- 本次捕获未执行语言切换操作，无法验证 `Locale` 切换、确认弹窗与重建逻辑。

## BAL（Background Activity Launch）

- `BAL hardening`、`would be blocked`、`Background activity launch` 等关键字命中 0 条。
- 发现 1 条允许的 BAL 启动：`BAL_ALLOW_ALLOWLISTED_UID` 从 `system` 启动模块设置 `MainActivity`，属于 LSPosed 管理器打开模块设置的标准行为，非异常。

## 内存样本

- `am_pss`：大量进程 PSS 采样，数量符合 Android 内存管理机制。
- `lmkd`：28 条记录；`am_low_memory`：2 条，位于系统启动早期（`20:39:07.314`）。
- 未发现 `OutOfMemoryError`。
- 模块进程 PID 28276/28606 未被 `lmkd` 或 `am_low_memory` 直接标记。

> 日志无法替代 Perfetto/Batterystats 量化内存/功耗；该类别结论仅为“无显著异常”。

## ROM / 框架噪声

- 2 条 `init` SIGABRT：root 级进程，无模块调用链。
- `init` 的 `SIGABRT` 与模块运行无时间/调用链关联。
- `MiuiCloudParsingCollection` / `SettingsLib` 的 `ClassNotFoundException` 属于 ROM 构建与 Cloud 数据解析差异，非模块注入导致。
- `SELinux` avc 拒绝（`priv_app` 访问 `vendor.qti.hardware.perf2.IPerf/default`）出现在 `com.android.mms` 进程加载第三方模块时，为 ROM SELinux 策略噪声，未导致模块加载失败。

## 源码定位建议

基于本次审查的低风险建议：

1. **RemotePreferences 可视化**：若后续要验证空快照/监听问题，在 `MainModule.java` 的 `initPrefs` / `watchPreferenceChange` 以及 `AppHelper.kt` 的 `remotePrefs` 初始化处增加带 `LSPosed-Bridge` tag 的日志，避免依赖框架默认输出。
2. **Settings 反射 fallback**：`com.android.settingslib.wifi.*` / `BroadcastSourceInfoHandler` 等类在部分 ROM 不存在，模块中若使用 `ClassUtils.getClass` 反射查找，应确保异常被捕获并返回空列表，不影响 Settings 基础功能。
3. **Launcher / system_server 验证**：本日志未捕获这两个进程的模块加载，若用户报告相关功能异常，需要重新抓取包含 `com.miui.home` 或 `system_server` 重新启动的 LSPosed 日志。
4. **Locale 实机验证**：语言切换未在日志中触发，建议按照 `docs/VERIFICATION.md` 中的 Locale 实机验证清单单独测试。

## 未证明 / 需补充事项

- RemotePreferences 初始化、监听回调与空快照：日志无直接证据。
- `system_server` 是否加载本模块：本日志未观察到。
- `com.miui.home`（Launcher）Hook 生命周期：本日志未触发。
- 语言切换状态稳定化、`AppLocaleController` 行为：未在捕获中操作。
- 模块功能是否全部正常：日志只能证明“无崩溃/无 ANR/无 Hook 失败”，不能证明每个功能均按预期工作。

## 结论

本次 `r14.13.3` 的 LSPosed 日志快速审查中：

- `tv.withaibuild.customiuizer.r14` 在 `com.android.settings:remote` 与 `com.android.systemui` 中均成功加载，入口类 `cp` 符合 R8 `-repackageclasses` 预期。
- 未发现可归因于本模块的 `FATAL EXCEPTION`、`am_crash`、`am_anr`、`ANR`、`Hook failed`、`NoSuchMethodError`、`NoSuchFieldError`。
- 13 条 `ClassNotFoundException` 为 ROM/框架/Settings 组件自身反射差异，栈中无模块调用链。
- 0 条 BAL 阻断、0 条 RemotePreferences 相关异常、0 条 Locale 相关异常。

因此，从日志证据看，本次捕获的 `r14.13.3` 版本在加载与运行时层面**未出现可归因于模块的 P0/P1 异常**。但功能正确性、API 102 独立环境、Launcher / system_server Hook 覆盖仍需按验证清单补充实机证据。
